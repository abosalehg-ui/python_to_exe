# خطة تحسين الواجهة وإضافة ميزات جديدة

> وثيقة تكميلية لـ [IDEAS.md](IDEAS.md). تلك الوثيقة تغطي كل خارطة الطريق منذ
> بداية المشروع؛ هذه الوثيقة تبدأ من حيث توقف آخر إصدار موثّق (Phase 8 في
> [CHANGELOG.md](CHANGELOG.md)) وتفصّل **تحسينات الواجهة تحديداً** ثم
> **ميزات جديدة** يمكن بناؤها فوق البنية الحالية.

## 1. أين يقف المشروع اليوم

البنية بعد Phase 8 صلبة: كود مقسّم (`core/` بلا PyQt5 + `ui/tabs/` مستقلة)،
329 اختبار، CI بـ ruff + pytest + pip-audit، i18n كامل (عربي/إنجليزي) بدون
إعادة تشغيل، سمتان (داكن/نهاري)، سحب وإفلات، dry-run، توقيع رقمي، Version
Info، Manifest، Splash، سجل بناءات، ومحرك Inno Setup كامل.

نقاط الضعف الملموسة بعد فحص الكود الحالي:

| الملاحظة | الموقع | الأثر |
|---------|--------|------|
| سمتان فقط، بدون اتّباع سمة النظام تلقائياً | `styles.py` (`THEMES = {"dark", "light"}`) | لا خيار للمستخدمين الذين يفضّلون Nord/Dracula، ولا اكتشاف تلقائي لسمة Windows/macOS |
| لا يوجد تكبير/تصغير للخط | لا وجود لإعداد حجم خط في `config.py`/`styles.py` | صعوبة على ضعاف البصر رغم أن `styles.py` يستخدم `pt` بالفعل (القاعدة موجودة) |
| لا System Tray ولا إشعارات نظام | لا استيراد لـ `QSystemTrayIcon` في المشروع كله | المستخدم يجب أن يبقى ناظراً للنافذة لمعرفة انتهاء بناء طويل |
| شريط التقدم تقديري (heuristic) وليس مبنياً على مراحل PyInstaller الفعلية | `conversion_thread.py` | لا مؤشر دقيق لوقت الانتظار المتبقي |
| تبويبات النشر/المثبّت (`deploy_tab.py`, `installer_tab.py`) تظهر بالكامل على Linux/macOS رغم أنها Windows-only (`signtool.exe`, Inno Setup) | `ui/tabs/deploy_tab.py`, `installer_tab.py` | تجربة مربكة لمن يشغّل الأداة على غير Windows رغم أن `builder.py` نفسه cross-platform (يتعامل مع فاصل `--add-data` حسب `sys.platform`) |
| لا مسار "مبسّط" للمبتدئين — 8 تبويبات تظهر دفعة واحدة | `main_window.py` | حاجز دخول لمن يريد فقط "حوّل ملفي" بدون التعمق في UPX/Manifest/Code Signing |
| لا يوجد فحص تحديثات للتطبيق نفسه | لا كود لذلك | يبقى المستخدمون على إصدارات قديمة رغم تكرار الإصلاحات (8 مراحل حتى الآن) |

هذه الملاحظات هي أساس الأولويات أدناه، وليست تخميناً — كلها متحقَّق منها في
الكود الفعلي بتاريخ هذه الوثيقة.

---

## 2. تحسينات الواجهة (UI/UX) — Phase 9

### 9.1 وضعان: مبسّط (Wizard) ومتقدّم
- عند أول تشغيل: شاشة ترحيب بثلاث خطوات فقط (اختر الملف → اختر النوع →
  ابنِ)، تخفي تبويبات النشر/المتقدم/المثبّت خلف مفتاح "إظهار الخيارات
  المتقدمة" واحد.
- تُحفظ رغبة المستخدم (`config.py`) بحيث لا تتكرر الشاشة لاحقاً، مع رابط
  "العودة للوضع المبسّط" من قائمة الإعدادات.
- **الملفات المتأثرة:** `ui/main_window.py` (تبديل مجموعة التبويبات
  المرئية)، `core/config.py` (حقل `simple_mode: bool` جديد)، `ui/dialogs.py`
  (حوار الترحيب لمرة واحدة).

### 9.2 تكييف تبويبات النشر/المثبّت حسب نظام التشغيل الحالي
- عند التشغيل على Linux/macOS: تبويب "📦 المثبّت" و"🚀 النشر" يعرضان تنبيهاً
  واضحاً بأن التوقيع/Inno Setup خاصان بـ Windows فقط بدل تعطيل صامت أو ظهور
  عادي مضلِّل، مع اقتراح البديل المناسب (مثال: `hdiutil`/`pkgbuild` على
  macOS مستقبلاً — انظر 3.7).
- **الملفات المتأثرة:** `ui/tabs/deploy_tab.py`, `ui/tabs/installer_tab.py`.

### 9.3 سمة تتبع نظام التشغيل + سمات إضافية
- إضافة `"auto"` كخيار سمة يقرأ تفضيل النظام (Windows/macOS dark mode) عند
  الإقلاع ويعيد التقييم عند استئناف التطبيق من الخلفية.
- توسيع `THEMES` في `styles.py` بسمتي Nord وSolarized (نفس بنية الألوان
  المُختبرة حالياً لـ WCAG AA — القالب جاهز، يحتاج فقط قيم ألوان جديدة
  ومقارنتها بنفس اختبار التباين الموجود).
- **الملفات المتأثرة:** `styles.py`, `core/config.py` (قيمة `theme` تقبل
  `auto`), اختبار جديد `tests/test_theme_contrast.py` يُوسَّع ليغطي السمتين
  الجديدتين.

### 9.4 تكبير/تصغير الخط (Zoom)
- زر `Ctrl++` / `Ctrl+-` / `Ctrl+0` يضرب كل أحجام الخط في `styles.py` (نسبة
  مئوية محفوظة في `config.py`)، بما أن الأنماط تستخدم `pt` بالفعل فالتغيير
  محصور في دالة `themed_stylesheet()` (تمرير معامل `scale`).
- **الملفات المتأثرة:** `styles.py`, `ui/main_window.py` (اختصارات لوحة
  المفاتيح الجديدة تُضاف لجدول الاختصارات في `strings.py`).

### 9.5 أيقونة في System Tray + إشعارات نظام
- `QSystemTrayIcon` يظهر عند بدء بناء يستغرق أكثر من X ثانية، مع إشعار عند
  الانتهاء (نجاح/فشل) حتى لو كانت النافذة مصغّرة أو خلف نوافذ أخرى.
- قائمة سياق بسيطة: إظهار النافذة / إلغاء البناء الحالي / خروج.
- **الملفات المتأثرة:** `ui/main_window.py` (تهيئة `QSystemTrayIcon` في
  `__init__`)، ملف جديد اختياري `ui/tray.py` إذا كبر المنطق.

### 9.6 شريط تقدم حقيقي بدل heuristic
- تحليل مخرجات PyInstaller (تحتوي على أسطر مثل `INFO: Analyzing ...`, `INFO:
  Building EXE ...`) في `conversion_thread.py` لتحديد المرحلة الفعلية (تحليل
  → تجميع → بناء EXE) بدل شريط زمني تقديري، مع عرض اسم المرحلة الحالية فوق
  الشريط.
- **الملفات المتأثرة:** `ui/conversion_thread.py`, `core/log_formatter.py`
  (تصنيف الأسطر حسب المرحلة).

### 9.7 معاينة الأيقونة بأحجام متعددة
- عند اختيار ملف `.ico`، عرض شبكة مصغّرة (16/32/48/256px) بدل الاعتماد فقط
  على مسار الملف كنص، لتفادي مشكلة "الأيقونة لا تظهر بشكل صحيح" الموثّقة
  في قسم حل المشاكل بالـ README.
- **الملفات المتأثرة:** `ui/tabs/main_tab.py`.

### 9.8 تحسين السجل: فلاتر وطي/فتح
- أزرار فلترة سريعة (الكل / أخطاء فقط / تحذيرات / معلومات) فوق مربع البحث
  الحالي، بدل الاعتماد على البحث النصي فقط.
- طي تلقائي لأسطر PyInstaller المتكررة (قوائم الوحدات المكتشفة الطويلة)
  خلف "عرض التفاصيل (٣٤٠ سطر)".
- **الملفات المتأثرة:** `ui/main_window.py` (منطقة السجل)، `core/log_formatter.py`.

### 9.9 إمكانية وصول أعمق
- وضع تباين عالٍ (High-contrast) كسمة ثالثة صريحة، وليس فقط سمة داكنة/فاتحة.
- التأكد أن كل عناصر التحكم الجديدة (فلاتر السجل، Tray، الخطوات) تحمل
  `setAccessibleName`/`setAccessibleDescription` بنفس الانضباط الذي طُبِّق
  على أزرار "📂" في Phase 8.
- **الملفات المتأثرة:** `styles.py`, كل تبويب جديد.

---

## 3. ميزات جديدة (Functional) — Phase 10+

### 10.1 التحويل الدفعي (Batch Conversion)
- تبويب جديد "📚 دفعي": قائمة ملفات `.py` متعددة، تطبيق نفس الإعدادات
  (الأيقونة، الخيارات، Version Info) على الجميع، وتقرير نهائي بنجاح/فشل كل
  ملف مع رابط لسجل كل عملية على حدة.
- إعادة استخدام `ConversionThread` الحالي داخل قائمة انتظار (queue) بدل
  فتحه لكل ملف بالتوازي، لتفادي تنافس PyInstaller على نفس مجلد `build/`.
- **ملفات جديدة:** `ui/tabs/batch_tab.py`, `core/batch_runner.py`.

### 10.2 إدارة البيئات الافتراضية ومفسّر بايثون
- اكتشاف `venv`/`.venv`/`conda env` داخل مجلد المشروع، قائمة منسدلة لاختيار
  المفسّر الذي يُستخدم لتشغيل PyInstaller (بدل الاعتماد الضمني على مفسّر
  التطبيق نفسه)، وزر "تثبيت PyInstaller في هذه البيئة" يتبع نفس مبدأ الموافقة
  الصريحة الذي طُبِّق في Phase 8 (لا تثبيت صامت).
- **ملفات جديدة:** `core/venv_manager.py`، تعديل `core/builder.py` لقبول
  مسار مفسّر مخصص عند بناء الأمر.

### 10.3 دعم المشاريع متعددة الملفات
- اختيار مجلد مشروع كامل بدل ملف واحد، مع اكتشاف نقطة الدخول (`main.py`,
  `__main__.py`, أو الملف الذي يحوي `if __name__ == "__main__"`) واقتراحها،
  وإضافة باقي المجلد تلقائياً عبر `--add-data` بذكاء (استثناء `.git`,
  `__pycache__`, `venv/`).
- **الملفات المتأثرة:** `ui/tabs/main_tab.py`, `core/builder.py`.

### 10.4 محدّث ذاتي للتطبيق
- فحص دوري (وليس عند كل إقلاع لتفادي طلبات شبكة غير متوقعة) لأحدث إصدار عبر
  GitHub Releases API، مع شارة "تحديث متوفر" في تبويب "حول البرنامج" تفتح
  رابط الإصدار — بدون تنزيل/تنفيذ تلقائي، تفادياً لمخاطر التحديث الصامت
  (نفس منطق "لا تثبيت صامت" المطبَّق على PyInstaller في Phase 8).
- **ملفات جديدة:** `core/update_checker.py`.

### 10.5 تكامل VirusTotal اختياري
- زر بعد نجاح البناء: "فحص عبر VirusTotal" يرفع الـ EXE (أو يحسب hash أولاً
  ويتحقق إن كان مفحوصاً سابقاً لتفادي رفع ملفات كبيرة دون داعٍ) عبر مفتاح
  API يُدخله المستخدم بنفسه ويُحفظ محلياً فقط. تعطيل الميزة افتراضياً حتى
  يوافق المستخدم صراحة (نفس نمط الموافقة الصريحة المتّبع في المشروع).
- **ملفات جديدة:** `core/virustotal_client.py`.

### 10.6 محرر ملف `.spec` للمستخدمين المتقدمين
- زر "تصدير كملف .spec" يولّد سكربت PyInstaller spec كامل من الإعدادات
  الحالية (بديل نصّي قابل للتعديل اليدوي لمستخدمي PyInstaller المتمرّسين)،
  وزر "استيراد من .spec" بالاتجاه المعاكس.
- **ملفات جديدة:** `core/spec_generator.py`.

### 10.7 استيراد/تصدير إعدادات كملف مشاركة (Presets)
- الميزة الحالية لحفظ/تحميل JSON موجودة في تبويب القوالب؛ التوسعة المقترحة:
  مكتبة "presets" محلية قابلة للتسمية والمشاركة كملف واحد، تظهر في تبويب
  القوالب بجانب القوالب الجاهزة الـ 11.
- **الملفات المتأثرة:** `ui/tabs/templates_tab.py`, `core/config.py`.

### 10.8 محلّل حجم البناء
- بعد بناء ناجح: تحليل مجلد `dist/` وعرض أكبر 10 وحدات/مكتبات من حيث الحجم،
  مع اقتراح `--exclude-module` للمكتبات غير المستخدمة فعلياً (استناداً إلى
  محلل التبعيات AST الموجود أصلاً في `core/dependency_analyzer.py`).
- **ملفات جديدة:** `core/build_size_analyzer.py`.

### 10.9 دعم تعبئة Linux/macOS رسمياً
- توسيع ما بعد البناء (`core/installer.py` حالياً مخصص لـ Inno Setup/Windows)
  بمسارات مكافئة: `AppImage`/`.deb` لينكس، `.dmg`/`pkgbuild` لماك، خلف نفس
  واجهة "📦 المثبّت" مع اكتشاف نظام التشغيل الحالي لعرض الخيار المناسب فقط
  (مرتبط مباشرة بالبند 9.2 أعلاه).
- **ملفات جديدة:** `core/installer_linux.py`, `core/installer_macos.py`.

### 10.10 الانتقال من PyQt5 إلى PySide6
- دافع الترخيص أساساً (PySide6 = LGPL، يسمح بمشاريع مغلقة المصدر تعتمد على
  الأداة دون قيود GPL)، إضافة إلى دعم Qt6 الأطول أمداً. مسار مقترح: طبقة
  توافق (`ui/qt_compat.py`) تستورد من PyQt5 أو PySide6 حسب المتوفر، تُشغَّل
  الاختبارات (`pytest-qt`) تحت كليهما في CI لمرحلة انتقالية قبل الحذف الكامل
  لـ PyQt5.
- **الملفات المتأثرة:** كل ملف يستورد `from PyQt5 import ...` (استبدال
  عبر طبقة التوافق أولاً، ثم إزالة تدريجية).

---

## 4. خارطة زمنية مقترحة

| المرحلة | التركيز | البنود | الجهد التقديري |
|--------|---------|--------|----------------|
| **Phase 9** | تحسينات الواجهة فقط | 9.1 → 9.9 | ~2 أسبوع |
| **Phase 10a** | ميزات إنتاجية سريعة الأثر | 10.1 (دفعي)، 10.4 (تحديث ذاتي)، 10.7 (Presets) | ~1.5 أسبوع |
| **Phase 10b** | ميزات متقدمة للمطورين | 10.2 (venv)، 10.3 (مشاريع متعددة الملفات)، 10.6 (.spec) | ~2 أسبوع |
| **Phase 10c** | نشر متعدد المنصات | 9.2، 10.9 | ~2-3 أسبوع (يتطلب اختبار فعلي على macOS/Linux) |
| **Phase 11** | جودة وأمان طويلة المدى | 10.5 (VirusTotal)، 10.8 (محلل الحجم)، 10.10 (PySide6) | مستمر / حسب الحاجة |

---

## 5. أولويات (قيمة مقابل جهد)

| الأولوية | البند | لماذا الآن |
|---------|------|-----------|
| 🔴 عالية جداً | 9.1 الوضع المبسّط للمبتدئين | أكبر حاجز دخول حالي: 8 تبويبات دفعة واحدة |
| 🔴 عالية جداً | 9.2 إخفاء/تنبيه ميزات Windows-only على أنظمة أخرى | يمنع تجربة مضلِّلة لمستخدمي Linux/macOS الحاليين فعلياً |
| 🟠 عالية | 10.1 التحويل الدفعي | مطلوب متكرر لمستخدمي عدة سكربتات |
| 🟠 عالية | 9.6 شريط تقدم حقيقي | يحل شكوى شائعة في أدوات تغليف مشابهة ("هل التطبيق عالق؟") |
| 🟡 متوسطة | 9.5 System Tray | راحة، ليست حاجة جوهرية |
| 🟡 متوسطة | 10.4 التحديث الذاتي | يفيد نشر الإصلاحات لكنه ليس عاجلاً |
| 🟢 منخفضة | 10.5 VirusTotal، 9.3 سمات إضافية | جمالي/تكميلي |
| 🟢 منخفضة (لكن استراتيجي) | 10.10 PySide6 | لا ضرر من التأجيل لكن كلما طال العمر زاد حجم الترحيل |

---

## 6. اعتبارات ومخاطر

1. **لا تغيير سلوك صامت**: كل ميزة تلمس الشبكة (تحديث ذاتي، VirusTotal) أو
   تنفّذ كوداً خارجياً (تثبيت PyInstaller في venv) يجب أن تتبع نفس مبدأ
   الموافقة الصريحة المطبَّق في Phase 8 — لا استثناءات.
2. **الاختبارات أولاً**: أي دالة جديدة في `core/` (batch_runner,
   venv_manager, spec_generator, build_size_analyzer) تُكتب مع اختبارات
   وحدة قبل ربطها بالواجهة، بنفس نمط `tests/test_builder.py` الحالي —
   يحافظ على تغطية `--cov-fail-under=90` في CI.
3. **لا كسر لتوافق الإعدادات المحفوظة**: أي حقل جديد في `BuildConfig`
   (`core/config.py`) يجب أن يقرأ بقيمة افتراضية آمنة من ملفات JSON قديمة،
   بنفس نمط `upx_level` المُبقى بعد استبداله بـ `upx_dir`.
4. **ترحيل PySide6 هو الأكبر خطورة**: يمسّ كل ملف تقريباً؛ يُنفَّذ عبر طبقة
   توافق واختبار مزدوج في CI قبل أي حذف، وليس كتبديل دفعة واحدة.

---

## 7. الخطوات التالية

1. مراجعة هذه الوثيقة وتحديد أي البنود تُعتمد الآن مقابل ما يُؤجَّل.
2. فتح issue منفصل لكل بند مُعتمَد على GitHub (تسهّل التتبع والمساهمة).
3. البدء بـ Phase 9 (تحسينات الواجهة) قبل Phase 10 — الأثر على تجربة
   المستخدم الحالي أعلى، والمخاطر التقنية أقل من ميزات تمسّ الشبكة أو أنظمة
   تشغيل أخرى.
